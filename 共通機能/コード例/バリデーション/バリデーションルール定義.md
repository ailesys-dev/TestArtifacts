```typescript
import useVuelidate from "@vuelidate/core";
import { helpers, maxLength, required } from "@vuelidate/validators";

/** [必須] 検索条件用バリデーションルール */
const searchConditionValidationRule = {
  nendo: {
    // [必須] MD仕様書「必須」列が●の場合
    required: helpers.withMessage(
      () => t("validation.required", { property: t("tblBudaimoku.nendo") }),
      required,
    ),
    // [必須] MD仕様書「Max」列に数値がある場合
    maxLength: helpers.withMessage(
      () =>
        t("validation.maxLength", {
          property: t("tblBudaimoku.nendo"),
          length: "4",
        }),
      maxLength(4),
    ),
  },
  shozoku1Id: {}, // [必須] バリデーション不要でも空ルール{}は必要
};

/** [必須] Vuelidate初期化 */
const vs$ = useVuelidate(searchConditionValidationRule, searchCondition, {
  $scope: "searchCondition_TblBudaimoku",
});
```
