```typescript
// ------------------------------------------------------
// ------------------------------------------------------
//パターンA: AlErrorMessageList（項目下の赤文字）
<template>
    <div class="itemflex-box">
        <AlLabel :itemName="t('tblBukamokukubun.kubuncode')" />
        // [必須] 必須項目には :required="true" を付ける
        <AlTextBoxANK
            v-model="editingItem.kubuncode"
            :required="true"
            :maxLength="7" />
        // [必須] 項目の直下にAlErrorMessageListを配置
        <AlErrorMessageList :varidator="v$.editingItem.kubuncode" />
    </div>
</template>

// ------------------------------------------------------
// ------------------------------------------------------
//パターンB: ElNotification（トースト一括表示）

import { h, toValue } from "vue";
import { ElNotification } from "element-plus";

/** [必須] 検索ボタン押下時のバリデーション */
const OnClickSearchButton = async () => {
    // [必須] バリデーション実行
    vs$.value.$touch();

    // [必須] エラーチェック＆トースト表示
    if (vs$.value.$invalid) {
        const messages: VNode[] = [];
        vs$.value.$errors.forEach((err) => {
            messages.push(h("li", null, toValue(err.$message)));
        });

        ElNotification({
            type: 'error',
            title: t('searchButtonOnClick.notification.error.title'),
            message: h("p", [
                h("div", t('searchButtonOnClick.notification.error.message')),
                h("ul", { class: "error-message" }, messages)
            ]),
        });
        return;  // [必須] エラー時は処理中断
    }

    // 検索処理続行...
};
```
