```html
<div class="searchitem">
  <!-- [必須] チェックボックス -->
  <AlCheckBox
    v-model="searchCondition.isCheckTarget"
    :itemName="t('label.target')"
    :disabled="!isBeforeLoadingTableItems"
  />

  <!-- [必須] ドロップダウン -->
  <AlComboBox
    v-model="searchCondition.targetId"
    :disabled="!isBeforeLoadingTableItems"
    :items="targetItems"
    @change="onTargetChange"
  />

  <!-- [推奨] クリアボタン -->
  <AlButton
    class="clearbutton"
    :itemName="t('label.clear')"
    :disabled="!isBeforeLoadingTableItems"
    @click="onClickTargetClear"
  />

  <!-- [必須] エラーメッセージ -->
  <ul v-if="vs$.targetId.$dirty && vs$.targetId.$invalid" class="error-message">
    <li v-for="msg in vs$.targetId.$errors" :key="msg.$uid">
      {{ msg.$message }}
    </li>
  </ul>
</div>
```
