```typescript
const OnClickSearchConditionClearButton = () => {
  // refで定義されているコンポーネントをConditionClearメソッドを呼び出し、初期化する。
  TblGakuseki_shozoku1IdRef.value?.ConditionClear();
  TblGakuseki_shozoku2IdRef.value?.ConditionClear();
  TblGakuseki_shozoku3IdRef.value?.ConditionClear();
  TblGakuseki_shozoku4IdRef.value?.ConditionClear();
  TblGakuseki_nenjiRef.value?.ConditionClear();
  TblGakuseki_code8Ref.value?.ConditionClear();

  // 以下のように、v-modelで定義されている値を初期化する方法もある。
  searchCondition_TblGakuseki.code8.value = null;
};
```
