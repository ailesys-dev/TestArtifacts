```typescript
const OnClickSearchButton = async () => {
  // [必須] バリデーションチェック
  vs$.value.$touch();
  if (vs$.value.$invalid) {
    // ElNotification表示
    return;
  }

  // [必須] ローディング表示して検索実行
  await alLoadingRef.value!.loadingScope(
    {
      cancelCallback: () => {
        loadCancelFlag.value = true;
      },
    },
    async () => {
      await loadTableItems_TblBudaimoku();
    },
  );
};
```
