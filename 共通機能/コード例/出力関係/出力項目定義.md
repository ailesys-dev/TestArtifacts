```typescript
/**  出力項目定義 */
const outputDefinitionMap = [
  // 基本的なフィールド
  { id: "code8", displayName: "学籍番号" },
  { id: "shimei", displayName: "氏名" },

  // ネストプロパティ（ドット記法）
  { id: "shozoku1Payload.meisyou", displayName: "部局" },

  // カスタムフォーマッタ（値の変換が必要な場合）
  {
    id: "status",
    displayName: "状態",
    formatter: (value) => (value === "1" ? "有効" : "無効"),
  },
] satisfies OutputDefinitionColumnArgs<TableRowType>;
```
